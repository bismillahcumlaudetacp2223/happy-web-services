apiVersion: v1
kind: Service
metadata:
  name: happy-feedback-service
spec:
  selector:
    app: happy-feedback-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
