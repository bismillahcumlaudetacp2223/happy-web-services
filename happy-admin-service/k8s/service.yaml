apiVersion: v1
kind: Service
metadata:
  name: happy-admin-service
spec:
  selector:
    app: happy-admin-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
