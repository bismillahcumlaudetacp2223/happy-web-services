apiVersion: v1
kind: Service
metadata:
  name: happy-product-service
spec:
  selector:
    app: happy-product-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
